name: knowledge-extract-ingest

on:
  push:
    paths:
      - "**/*.rs"
      - "**/*.py"
      - "docs/**"
      - ".github/workflows/knowledge-extract-ingest.yml"
  workflow_dispatch:

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/wgx-reusable.yml
