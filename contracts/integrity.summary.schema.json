{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.heimgewebe.org/contracts/integrity.summary.schema.json",
  "title": "IntegritySummary",
  "x-producers": ["wgx"],
  "x-consumers": ["leitstand", "chronik"],
  "description": "Zusammenfassender Integritätsbericht eines Repos.",
  "type": "object",
  "properties": {
    "repo": {
      "type": "string",
      "description": "Name des Repositories (z. B. heimgewebe/metarepo)."
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "Erzeugungszeitpunkt (ISO-8601)."
    },
    "status": {
      "type": "string",
      "enum": ["OK", "WARN", "FAIL", "MISSING", "UNCLEAR"],
      "description": "Globaler Integritätsstatus."
    },
    "counts": {
      "type": "object",
      "description": "Zählstatistiken.",
      "properties": {
        "claims": {
          "type": "integer",
          "minimum": 0
        },
        "artifacts": {
          "type": "integer",
          "minimum": 0
        },
        "loop_gaps": {
          "type": "integer",
          "minimum": 0
        },
        "unclear": {
          "type": "integer",
          "minimum": 0
        }
      },
      "required": ["claims", "artifacts", "loop_gaps", "unclear"],
      "additionalProperties": true
    },
    "details": {
      "type": "object",
      "description": "Detailinformationen zu den Zählungen.",
      "properties": {
        "claims": {
          "type": "array",
          "items": { "type": "string" }
        },
        "artifacts": {
          "type": "array",
          "items": { "type": "string" }
        },
        "loop_gaps": {
          "type": "array",
          "items": { "type": "string" }
        },
        "unclear": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "required": ["claims", "artifacts", "loop_gaps", "unclear"],
      "additionalProperties": true
    }
  },
  "required": ["repo", "generated_at", "status", "counts", "details"],
  "additionalProperties": false
}
